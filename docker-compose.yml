services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
  app:
    image: app
    ports:
      - "5020"
    depends_on:
      - "postgres"
    build:
      context: .
      dockerfile: Dockerfile
  gw:
    image: gw
    depends_on:
      - "postgres"
    build:
      context: .
      dockerfile: Dockerfile
  identity:
    image: identity
    ports:
      - "5010"
    depends_on:
      - "postgres"
    build:
      context: .
      dockerfile: Dockerfile